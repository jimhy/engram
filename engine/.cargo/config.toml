# 本机跑测试偶发 STATUS_ACCESS_VIOLATION，根因未定位（本机 rustc 编译也会随机
# AV，疑似环境/硬件层问题），保守强制测试单线程运行，保证套件稳定、不弹崩溃弹窗。
# 更正：此前注释归因为「redb Windows mmap 竞态」——经核实 redb 2.6.3 无任何
# mmap（Windows 后端是 seek_read/seek_write + LockFile），此前归因有误。
[env]
RUST_TEST_THREADS = "1"
